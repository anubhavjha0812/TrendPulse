# Deployed backend URL (Render), including the /api prefix.
# Set this in Vercel's project settings -> Environment Variables.
# Leave unset for local dev — it falls back to http://localhost:8000/api.
VITE_API_BASE_URL=https://your-backend.onrender.com/api
